@inherits LayoutComponentBase
@inject NavigationManager NavigationManager

<div class="min-h-screen bg-gray-50">
    <!-- Header/Navigation -->
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between h-16">
                <div class="flex">
                    @* <div class="flex-shrink-0 flex items-center">
                        <NavLink href="" class="text-xl font-bold text-indigo-600">
                            Forum Studenti
                        </NavLink>
                    </div> *@
             @*        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                        <NavLink href="forum" 
                                 class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium @(GetNavLinkClass("forum"))">
                            <i class="fas fa-comments mr-2"></i> Forum
                        </NavLink>
                        <NavLink href="fileshare" 
                                class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium @(GetNavLinkClass("fileshare"))">
                            <i class="fas fa-folder-tree mr-2"></i> File Share
                        </NavLink>
                    </div> *@
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:items-center space-x-4">
                    <button class="text-gray-500 hover:text-indigo-600">
                        <i class="fas fa-bell text-xl"></i>
                    </button>
                    <button class="bg-indigo-600 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-indigo-700 transition duration-150">
                        Accedi
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        @Body
    </main>
</div>

@code {
    private string GetNavLinkClass(string page)
    {
        var relativePath = NavigationManager.ToBaseRelativePath(NavigationManager.Uri).ToLower();
        var isActive = relativePath.StartsWith(page);
        return isActive ? 
            "border-indigo-500 text-gray-900" : 
            "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700";
    }
}
